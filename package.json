{
  "name": "cf_ai_assistant",
  "version": "1.0.0",
  "description": "AI-powered chat assistant built on Cloudflare infrastructure",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "npm run build:worker && npm run build:pages",
    "build:worker": "wrangler deploy --dry-run",
    "build:pages": "npm run build --prefix frontend"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "keywords": ["cloudflare", "ai", "llama", "chat", "assistant"],
  "author": "Cloudflare Assignment",
  "license": "MIT"
}